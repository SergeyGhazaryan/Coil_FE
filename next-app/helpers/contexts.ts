import { createContext } from "react";

export const warningModalContext = createContext(null);
export const editWarningModalContext = createContext(null);
export const coilEditWarningModalContext = createContext(null);
